# Бэклог по тестированию программы "Журнал личных учебных достижений"

## 📋 Легенда приоритетов
- **🟠 Высокий** - серьезная проблема, влияет на корректность работы
- **🟡 Средний** - проблема, требующая исправления, но не блокирующая
- **🟢 Низкий** - косметическая проблема или улучшение

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ (P1)

### **BUG-101: Неверная сортировка при одинаковых датах**
- **Статус:** 🟠 ВЫСОКИЙ
- **Описание:** При наличии записей с одинаковой датой, порядок их отображения недетерминирован
- **Где воспроизводится:** Функция `load_records()`
- **Влияние:** Пользователь видит записи в разном порядке при перезагрузке
- **Решение:** Добавить вторичную сортировку по ID или названию

```python
# БЫЛО:
cur.execute("SELECT дата, название, тип, уровень FROM достижения ORDER BY дата DESC")

# СТАЛО:
cur.execute("SELECT дата, название, тип, уровень FROM достижения ORDER BY дата DESC, id DESC")
```

### **BUG-102: Уязвимость к SQL-инъекциям в функции delete_record**
- **Статус:** 🟠 ВЫСОКИЙ
- **Описание:** Используется небезопасный способ формирования SQL-запроса
- **Где воспроизводится:** В тестовой функции `delete_record()`
- **Влияние:** Потенциальная уязвимость безопасности
- **Решение:** Использовать параметризованные запросы

```python
# БЫЛО (небезопасно):
def delete_record(selected_index):
    # ... получение ID ...
    cur.execute(f"DELETE FROM достижения WHERE id = {record_id}")

# СТАЛО (безопасно):
def delete_record(selected_index):
    # ... получение ID ...
    cur.execute("DELETE FROM достижения WHERE id = ?", (record_id,))
```

## 🟡 СРЕДНИЙ ПРИОРИТЕТ (P2)

### **BUG-201: Системные сообщения об ошибках в консоли**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** При ошибках загрузки types.json выводятся системные сообщения
- **Где воспроизводится:** В тестах T-006 и T-008
- **Влияние:** Ухудшает читаемость отчетов тестирования
- **Решение:** Добавить параметр для подавления системных сообщений

```python
# БЫЛО:
def load_types():
    try:
        with open("types.json", "r", encoding="utf-8") as f:
            return json.load(f)
    except:
        print("Ошибка загрузки types.json")
        return ["Олимпиада", "Сертификат", "Проект", "Экзамен", "Конференция"]

# СТАЛО:
def load_types(silent=False):
    try:
        with open("types.json", "r", encoding="utf-8") as f:
            return json.load(f)
    except Exception as e:
        if not silent:
            print(f"Ошибка загрузки types.json: {e}")
        return ["Олимпиада", "Сертификат", "Проект", "Экзамен", "Конференция"]
```

### **BUG-202: Отсутствие транзакций для групповых операций**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** При добавлении нескольких записей нет механизма отката при ошибке
- **Где воспроизводится:** В тестовом сценарии с несколькими записями
- **Влияние:** Возможность частичного сохранения данных
- **Решение:** Добавить поддержку транзакций

### **BUG-203: Невалидация формата даты**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** Дата принимается в любом формате, не только ГГГГ-ММ-ДД
- **Где воспроизводится:** В функции `save_to_db()`
- **Влияние:** Некорректная сортировка и фильтрация
- **Решение:** Добавить проверку формата даты

```python
def validate_date(date_str):
    try:
        datetime.datetime.strptime(date_str, "%Y-%m-%d")
        return True
    except ValueError:
        return False
```

### **BUG-204: Отсутствие уникальных ограничений в БД**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** Можно добавить несколько одинаковых записей
- **Влияние:** Дублирование данных
- **Решение:** Добавить UNIQUE constraint или проверку на дубли

## 🟢 НИЗКИЙ ПРИОРИТЕТ (P3)

### **BUG-301: Некорректные сообщения в тестах**
- **Статус:** 🟢 НИЗКИЙ
- **Описание:** В тестах выводятся русские символы в неправильной кодировке
- **Где воспроизводится:** В выводе тестов PyCharm
- **Влияние:** Ухудшает читаемость отчетов
- **Решение:** Настроить кодировку вывода в тестах

### **BUG-302: Неоптимальные индексы в БД**
- **Статус:** 🟢 НИЗКИЙ
- **Описание:** Отсутствуют индексы для часто используемых полей
- **Влияние:** Снижение производительности при большом объеме данных
- **Решение:** Добавить индексы для полей `дата`, `тип`, `уровень`

```sql
CREATE INDEX idx_дата ON достижения(дата DESC);
CREATE INDEX idx_тип ON достижения(тип);
CREATE INDEX idx_уровень ON достижения(уровень);
```

### **BUG-303: Отсутствие прогресса выполнения**
- **Статус:** 🟢 НИЗКИЙ
- **Описание:** При выполнении длительных операций нет индикатора прогресса
- **Влияние:** Пользователь не знает, выполняется ли операция
- **Решение:** Добавить индикатор выполнения для операций с БД

### **BUG-304: Неинформативные названия тестов**
- **Статус:** 🟢 НИЗКИЙ
- **Описание:** Названия тестов недостаточно описывают их назначение
- **Влияние:** Сложность поддержки тестов
- **Решение:** Переименовать тесты по стандарту `test_<что_тестируется>_<условие>_<ожидаемый_результат>`

## 📊 ОШИБКИ В ТЕСТАХ

### **TEST-BUG-401: Тесты не изолированы полностью**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** Тесты используют общие временные файлы
- **Влияние:** Возможны ложные срабатывания
- **Решение:** Использовать уникальные имена файлов для каждого теста

### **TEST-BUG-402: Отсутствие тестов на граничные условия**
- **Статус:** 🟡 СРЕДНИЙ
- **Описание:** Нет тестов для пустых строк, null значений, максимальных длин
- **Влияние:** Неполное покрытие кода
- **Решение:** Добавить тесты граничных условий

### **TEST-BUG-403: Тесты зависят от порядка выполнения**
- **Статус:** 🟢 НИЗКИЙ
- **Описание:** Некоторые тесты могут зависеть от состояния, оставленного предыдущими тестами
- **Влияние:** Нестабильность тестов
- **Решение:** Обеспечить полную изоляцию каждого теста

## 🎯 ПРЕДЛОЖЕНИЯ ПО УЛУЧШЕНИЮ (ENHANCEMENTS)

### **ENH-501: Добавить логирование операций**
- **Приоритет:** 🟡 СРЕДНИЙ
- **Описание:** Ведение лога всех операций с БД
- **Польза:** Упрощение отладки и аудита
- **Сложность:** Низкая

### **ENH-502: Кэширование загруженных типов**
- **Приоритет:** 🟢 НИЗКИЙ
- **Описание:** Кэшировать загруженные из JSON типы в памяти
- **Польза:** Повышение производительности
- **Сложность:** Средняя

### **ENH-503: Добавить миграции БД**
- **Приоритет:** 🟡 СРЕДНИЙ
- **Описание:** Поддержка обновления структуры БД между версиями
- **Польза:** Упрощение обновлений
- **Сложность:** Высокая

### **ENH-504: Автоматическое резервное копирование**
- **Приоритет:** 🟢 НИЗКИЙ
- **Описание:** Создание резервных копий БД при изменении данных
- **Польза:** Защита от потери данных
- **Сложность:** Средняя

## 📈 СТАТИСТИКА ОШИБОК

| Приоритет | Количество | % от общего |
|-----------|------------|-------------|
| 🟠 Высокий | 3 | 23% |
| 🟡 Средний | 7 | 54% |
| 🟢 Низкий | 3 | 23% |
| **Всего** | **13** | **100%** |

## 🚀 ПЛАН ИСПРАВЛЕНИЙ

### **Этап 1: Высокий приоритет (1 неделя)**
1. BUG-101 - Исправить сортировку (2 часа)
2. BUG-102 - Исправить SQL-инъекции (1 час)
3. BUG-103 - Добавить валидацию длины (3 часа)

### **Этап 2: Средний приоритет (2 недели)**
1. BUG-201 - Подавление системных сообщений (1 час)
2. BUG-202 - Добавить транзакции (4 часа)
3. BUG-203 - Валидация формата даты (2 часа)
4. TEST-BUG-401 - Изолировать тесты (3 часа)

### **Этап 3: Низкий приоритет (по мере возможности)**
1. BUG-301 - Исправить кодировку (1 час)
2. BUG-302 - Добавить индексы (2 часа)
3. ENH-501 - Добавить логирование (4 часа)

## 📋 КРИТЕРИИ КАЧЕСТВА ДО ИСПРАВЛЕНИЙ

| Метрика | Текущее значение | Целевое значение |
|---------|-----------------|------------------|
| Покрытие кода | 14.5% | 80% |
| Критические ошибки | 0 | 0 |
| Высокоприоритетные ошибки | 3 | 0 |
| Стабильность тестов | 100% | 100% |
| Производительность | <1 сек/тест | <1 сек/тест |

## 🎯 РЕКОМЕНДАЦИИ

### **Немедленные действия:**
1. Исправить BUG-101 и BUG-102 (критически важно)
2. Добавить валидацию входных данных
3. Улучшить изоляцию тестов

### **Долгосрочные улучшения:**
1. Внедрить CI/CD для автоматического тестирования
2. Добавить статический анализ кода
3. Реализовать мониторинг качества кода

---

**Дата составления:** 30.01.2026
**Ответственный за исправления:** Ларетина Д.